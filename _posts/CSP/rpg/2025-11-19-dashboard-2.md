---
layout: post
title: Dashboard
description: Complete content selection and creation interface for your RPG adventure
permalink: /rpg/dashboard
comments: True
sidebar: True 
---

<style>
/* Reset and Base Styles */
.page-content .wrapper,
.page-content {
    max-width: 100% !important;
    width: 100% !important;
    padding: 0 !important;
    margin: 0 auto !important;
}

body {
    font-family: 'Cinzel', 'Georgia', serif;
    background: #1a1a2e;
    min-height: 100vh;
    margin: 0;
    padding: 20px;
}

/* Main Container */
.main-container {
    max-width: 1400px;
    margin: 0 auto;
}

/* Minimal Header */

@keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-10px); }
}

/* Visual Grid - 3 cards per row */
.visual-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 30px;
    margin-bottom: 40px;
}

/* Visual Card */
.visual-card {
    height: 320px;
    border-radius: 15px;
    overflow: hidden;
    position: relative;
    cursor: pointer;
    transition: all 0.3s ease;
    background: linear-gradient(145deg, rgba(30, 30, 60, 0.95), rgba(20, 20, 40, 0.95));
    border: 2px solid rgba(255, 215, 0, 0.2);
    box-shadow: 
        0 8px 25px rgba(0, 0, 0, 0.4),
        inset 0 1px 0 rgba(255, 255, 255, 0.1);
}

.visual-card:hover {
    transform: translateY(-5px);
    border-color: rgba(255, 215, 0, 0.5);
    box-shadow: 
        0 15px 40px rgba(0, 0, 0, 0.6),
        0 0 40px rgba(255, 215, 0, 0.2),
        inset 0 1px 0 rgba(255, 255, 255, 0.2);
}

/* Visual Area - 85% */
.card-visual {
    height: 270px; /* 85% of 320px */
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
}

/* Main Icon */
.main-icon {
    font-size: 7em;
    filter: drop-shadow(0 0 15px currentColor);
    opacity: 0.9;
    z-index: 2;
    position: relative;
    transition: transform 0.3s ease;
}

.visual-card:hover .main-icon {
    transform: scale(1.1) rotate(5deg);
}

/* Background Effects */
.bg-effect {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    opacity: 0.3;
}

/* Different Colors for Each Card */
.visual-card:nth-child(1) {
    --card-color: #9B59B6;
}

.visual-card:nth-child(2) {
    --card-color: #3498DB;
}

.visual-card:nth-child(3) {
    --card-color: #2ECC71;
}

.visual-card:nth-child(4) {
    --card-color: #E67E22;
}

.visual-card:nth-child(5) {
    --card-color: #E74C3C;
}

.visual-card:nth-child(6) {
    --card-color: #95A5A6;
}

/* Apply Colors */
.visual-card:nth-child(1) .main-icon { color: #9B59B6; }
.visual-card:nth-child(2) .main-icon { color: #3498DB; }
.visual-card:nth-child(3) .main-icon { color: #2ECC71; }
.visual-card:nth-child(4) .main-icon { color: #E67E22; }
.visual-card:nth-child(5) .main-icon { color: #E74C3C; }
.visual-card:nth-child(6) .main-icon { color: #95A5A6; }

.visual-card:nth-child(1) .bg-effect { 
    background: radial-gradient(circle at 30% 30%, rgba(155, 89, 182, 0.2) 0%, transparent 70%);
}
.visual-card:nth-child(2) .bg-effect { 
    background: radial-gradient(circle at 70% 30%, rgba(52, 152, 219, 0.2) 0%, transparent 70%);
}
.visual-card:nth-child(3) .bg-effect { 
    background: radial-gradient(circle at 30% 70%, rgba(46, 204, 113, 0.2) 0%, transparent 70%);
}
.visual-card:nth-child(4) .bg-effect { 
    background: radial-gradient(circle at 70% 70%, rgba(230, 126, 34, 0.2) 0%, transparent 70%);
}
.visual-card:nth-child(5) .bg-effect { 
    background: radial-gradient(circle at 50% 50%, rgba(231, 76, 60, 0.2) 0%, transparent 70%);
}
.visual-card:nth-child(6) .bg-effect { 
    background: radial-gradient(circle at 50% 30%, rgba(149, 165, 166, 0.2) 0%, transparent 70%);
}

/* Title Area - 15% */
.card-title {
    height: 50px; /* 15% of 320px */
    background: rgba(0, 0, 0, 0.7);
    backdrop-filter: blur(5px);
    display: flex;
    align-items: center;
    justify-content: center;
    border-top: 1px solid rgba(255, 215, 0, 0.1);
}

.section-name {
    color: #ffd700;
    font-size: 1.1em;
    font-weight: bold;
    letter-spacing: 1px;
    text-transform: uppercase;
}

/* Quick Actions */
.actions-row {
    display: flex;
    justify-content: center;
    gap: 20px;
    margin-top: 40px;
}

.action-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
    cursor: pointer;
    transition: all 0.3s ease;
    padding: 10px;
}

.action-icon {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: rgba(255, 215, 0, 0.1);
    border: 2px solid rgba(255, 215, 0, 0.3);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.2em;
    color: #ffd700;
    transition: all 0.3s ease;
}

.action-label {
    color: #c0c0c0;
    font-size: 0.8em;
    text-align: center;
}

.action-item:hover .action-icon {
    background: rgba(255, 215, 0, 0.2);
    border-color: #ffd700;
    transform: scale(1.1);
    box-shadow: 0 0 15px rgba(255, 215, 0, 0.3);
}

.action-item:hover .action-label {
    color: #ffd700;
}

/* Hover Tooltip */
.visual-card::after {
    content: attr(data-tooltip);
    position: absolute;
    bottom: 80px;
    left: 50%;
    transform: translateX(-50%);
    background: rgba(20, 20, 40, 0.9);
    color: #ffd700;
    padding: 6px 10px;
    border-radius: 6px;
    font-size: 0.8em;
    opacity: 0;
    transition: opacity 0.3s ease;
    pointer-events: none;
    white-space: nowrap;
    border: 1px solid rgba(255, 215, 0, 0.3);
    z-index: 10;
}

.visual-card:hover::after {
    opacity: 1;
}

/* Responsive */
@media (max-width: 1200px) {
    .visual-grid {
        grid-template-columns: repeat(3, 1fr);
        gap: 20px;
    }
}

@media (max-width: 900px) {
    .visual-grid {
        grid-template-columns: repeat(2, 1fr);
    }
}

@media (max-width: 600px) {
    .visual-grid {
        grid-template-columns: 1fr;
        max-width: 400px;
        margin: 0 auto 40px;
    }
    
    .visual-card {
        height: 280px;
    }
    
    .card-visual {
        height: 240px; /* 85% of 280px */
    }
    
    .card-title {
        height: 40px; /* 15% of 280px */
    }
    
    .main-icon {
        font-size: 5em;
    }
    
    .actions-row {
        flex-wrap: wrap;
    }
}

/* Animations */
@keyframes cardPulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.02); }
}

.visual-card:hover {
    animation: cardPulse 2s infinite;
}
</style>

<div class="main-container">
    <!-- Visual Grid - 3 per row -->
    <div class="visual-grid">
        <!-- Story -->
        <div class="visual-card" 
             data-tooltip="Story & Narrative"
             onclick="navigateTo('story')">
            <div class="card-visual">
                <div class="bg-effect"></div>
                <div class="main-icon">📖</div>
            </div>
            <div class="card-title">
                <span class="section-name">Story</span>
            </div>
        </div>

        <!-- Game Creator -->
        <div class="visual-card" 
             data-tooltip="Game Creator"
             onclick="navigateTo('game-creator')">
            <div class="card-visual">
                <div class="bg-effect"></div>
                <div class="main-icon">🎮</div>
            </div>
            <div class="card-title">
                <span class="section-name">Game</span>
            </div>
        </div>

        <!-- Controls -->
        <div class="visual-card" 
             data-tooltip="Controls"
             onclick="navigateTo('controls')">
            <div class="card-visual">
                <div class="bg-effect"></div>
                <div class="main-icon">🎯</div>
            </div>
            <div class="card-title">
                <span class="section-name">Controls</span>
            </div>
        </div>

        <!-- Systems -->
        <div class="visual-card" 
             data-tooltip="Game Systems"
             onclick="navigateTo('systems')">
            <div class="card-visual">
                <div class="bg-effect"></div>
                <div class="main-icon">⚙️</div>
            </div>
            <div class="card-title">
                <span class="section-name">Systems</span>
            </div>
        </div>

        <!-- Review -->
        <div class="visual-card" 
             data-tooltip="Review & Publish"
             onclick="navigateTo('review')">
            <div class="card-visual">
                <div class="bg-effect"></div>
                <div class="main-icon">📋</div>
            </div>
            <div class="card-title">
                <span class="section-name">Review</span>
            </div>
        </div>

        <!-- Tools -->
        <div class="visual-card" 
             data-tooltip="Additional Tools"
             onclick="navigateTo('tools')">
            <div class="card-visual">
                <div class="bg-effect"></div>
                <div class="main-icon">🧰</div>
            </div>
            <div class="card-title">
                <span class="section-name">Tools</span>
            </div>
        </div>
    </div>

    <!-- Quick Actions -->
    <div class="actions-row">
        <div class="action-item" onclick="saveAll()">
            <div class="action-icon">💾</div>
            <div class="action-label">Save</div>
        </div>
        <div class="action-item" onclick="exportProject()">
            <div class="action-icon">📤</div>
            <div class="action-label">Export</div>
        </div>
        <div class="action-item" onclick="showHelp()">
            <div class="action-icon">❓</div>
            <div class="action-label">Help</div>
        </div>
        <div class="action-item" onclick="refreshDashboard()">
            <div class="action-icon">🔄</div>
            <div class="action-label">Refresh</div>
        </div>
    </div>
</div>

<script>
// Dashboard Manager
class Dashboard {
    constructor() {
        this.sections = [
            { id: 'story', name: 'Story', icon: '📖', color: '#9B59B6', path: '/rpg/story' },
            { id: 'game-creator', name: 'Game', icon: '🎮', color: '#3498DB', path: '/rpg/game-creator' },
            { id: 'controls', name: 'Controls', icon: '🎯', color: '#2ECC71', path: '/rpg/controls' },
            { id: 'systems', name: 'Systems', icon: '⚙️', color: '#E67E22', path: '/rpg/systems' },
            { id: 'review', name: 'Review', icon: '📋', color: '#E74C3C', path: '/rpg/review' },
            { id: 'tools', name: 'Tools', icon: '🧰', color: '#95A5A6', path: '/rpg/tools' }
        ];
        
        this.init();
    }
    
    init() {
        this.setupCardInteractions();
        this.setupTooltips();
    }
    
    setupCardInteractions() {
        const cards = document.querySelectorAll('.visual-card');
        
        cards.forEach((card, index) => {
            const section = this.sections[index];
            
            // Click effect
            card.addEventListener('click', () => {
                this.animateCardClick(card);
                
                // Navigate after animation
                setTimeout(() => {
                    window.location.href = section.path;
                }, 300);
            });
            
            // Create subtle glow effect
            card.addEventListener('mouseenter', () => {
                this.animateCardHover(card, section);
            });
        });
    }
    
    animateCardClick(card) {
        // Scale down
        card.style.transform = 'scale(0.95)';
        
        // Add glow effect
        card.style.boxShadow = `
            0 0 60px rgba(255, 215, 0, 0.6),
            0 15px 40px rgba(0, 0, 0, 0.7)
        `;
        
        // Reset after animation
        setTimeout(() => {
            card.style.transform = '';
            card.style.boxShadow = '';
        }, 300);
    }
    
    animateCardHover(card, section) {
        // Create floating particles on hover
        if (!card._particlesCreated) {
            this.createHoverParticles(card, section.color);
            card._particlesCreated = true;
        }
    }
    
    createHoverParticles(card, color) {
        const visualArea = card.querySelector('.card-visual');
        
        for (let i = 0; i < 5; i++) {
            const particle = document.createElement('div');
            particle.style.cssText = `
                position: absolute;
                width: 4px;
                height: 4px;
                background: ${color};
                border-radius: 50%;
                pointer-events: none;
                opacity: 0;
                filter: blur(1px);
            `;
            
            visualArea.appendChild(particle);
            
            // Animate particle
            const x = Math.random() * 100;
            const y = Math.random() * 100;
            const duration = Math.random() * 1 + 1;
            
            particle.style.left = `${x}%`;
            particle.style.top = `${y}%`;
            particle.style.animation = `
                particleFloat ${duration}s ease-in-out infinite
            `;
        }
        
        // Add animation keyframes
        if (!document.getElementById('particle-animations')) {
            const style = document.createElement('style');
            style.id = 'particle-animations';
            style.textContent = `
                @keyframes particleFloat {
                    0%, 100% { 
                        opacity: 0; 
                        transform: translateY(0) rotate(0deg); 
                    }
                    50% { 
                        opacity: 0.6; 
                        transform: translateY(-20px) rotate(180deg); 
                    }
                }
            `;
            document.head.appendChild(style);
        }
    }
    
    setupTooltips() {
        // Tooltips are already handled by CSS
        // Add any additional tooltip logic here if needed
    }
    
    // Public methods
    saveAll() {
        // Create visual feedback
        const saveBtn = document.querySelector('.action-item:nth-child(1) .action-icon');
        saveBtn.textContent = '✅';
        saveBtn.style.background = 'rgba(46, 204, 113, 0.2)';
        saveBtn.style.borderColor = '#2ECC71';
        
        setTimeout(() => {
            saveBtn.textContent = '💾';
            saveBtn.style.background = '';
            saveBtn.style.borderColor = '';
        }, 1000);
    }
    
    exportProject() {
        // Visual feedback
        const exportBtn = document.querySelector('.action-item:nth-child(2) .action-icon');
        const originalText = exportBtn.textContent;
        exportBtn.textContent = '⏳';
        exportBtn.style.background = 'rgba(52, 152, 219, 0.2)';
        exportBtn.style.borderColor = '#3498DB';
        
        setTimeout(() => {
            exportBtn.textContent = '📤';
            exportBtn.style.background = '';
            exportBtn.style.borderColor = '';
            alert('Export complete!');
        }, 1500);
    }
    
    showHelp() {
        // Simple help modal
        const helpContent = `
            RPG Creator Dashboard
            ---------------------
            • Click any card to open
            • Hover for tooltips
            • Progress shown in bottom right
            • All sections are accessible
            • Save frequently
            • Export when ready
        `;
        
        alert(helpContent);
    }
    
    refreshDashboard() {
        // Visual feedback
        const refreshBtn = document.querySelector('.action-item:nth-child(4) .action-icon');
        refreshBtn.style.animation = 'spin 1s linear';
        
        setTimeout(() => {
            refreshBtn.style.animation = '';
            location.reload();
        }, 1000);
        
        // Add spin animation
        if (!document.getElementById('spin-animation')) {
            const style = document.createElement('style');
            style.id = 'spin-animation';
            style.textContent = `
                @keyframes spin {
                    0% { transform: rotate(0deg); }
                    100% { transform: rotate(360deg); }
                }
            `;
            document.head.appendChild(style);
        }
    }
}

// Initialize dashboard
document.addEventListener('DOMContentLoaded', () => {
    window.dashboard = new Dashboard();
});

// Global navigation function
function navigateTo(sectionId) {
    const sections = {
        'story': '/rpg/story',
        'game-creator': '/rpg/game-creator',
        'controls': '/rpg/controls',
        'systems': '/rpg/systems',
        'review': '/rpg/review',
        'tools': '/rpg/tools'
    };
    
    if (sections[sectionId]) {
        window.location.href = sections[sectionId];
    }
}

// Global action functions
function saveAll() {
    if (window.dashboard) {
        window.dashboard.saveAll();
    }
}

function exportProject() {
    if (window.dashboard) {
        window.dashboard.exportProject();
    }
}

function showHelp() {
    if (window.dashboard) {
        window.dashboard.showHelp();
    }
}

function refreshDashboard() {
    if (window.dashboard) {
        window.dashboard.refreshDashboard();
    }
}
</script>